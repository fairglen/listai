{
  "name": "listai",
  "version": "0.1.0",
  "description": "AI-powered property search pipeline for Portugal (Idealista, Imovirtual, Casa Sapo, etc).",
  "type": "module",
  "private": true,
  "scripts": {
    "export": "node export-xlsx.mjs"
  },
  "_comment": "Agent-driven (Markdown modes + YAML config). The only shipped script is export-xlsx.mjs (CSV/TSV -> XLSX), which uses Node built-ins ONLY — zero dependencies, no install. Keep it that way. Usage: npm run export -- <output.xlsx> <input.csv|tsv> [more...]",
  "dependencies": {}
}
